import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

const inter = Inter({
  subsets: ["latin"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "KASST | Revolutionizing Online Video Platforms with Blockchain",
  description: "K2S is revolutionizing online video platforms with blockchain technology, empowering creators, rewarding viewers, and transforming advertising.",
  keywords: "K2S, KASST, cryptocurrency, blockchain, video platform, token, creators, viewers, advertising",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={`${inter.variable} font-sans antialiased`}>
        <Navbar />
        <main>{children}</main>
        <Footer />
      </body>
    </html>
  );
}
